import { styled } from '@mui/system';

export const TermsGeneratorWrapper = styled('div')({
  backgroundColor: '#FDFDFD',
  margin: '3.5rem 0rem',
});

export const TermsGeneratorContent = styled('div')({});

export const TextBox = styled('div')({
  marginBottom: '1rem',
});
