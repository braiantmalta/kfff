import { styled } from '@mui/system';

export const BlackFooterWrapper = styled('div')({
  backgroundColor: '#1D1D1D',
  width: '100%',
  height: '90px',
  display: 'flex',
  alignItems: 'center',
});

export const BlackFooterContent = styled('div')({
  display: 'flex',
  alignItems: 'center',
  flexDirection: 'column',
  justifyContent: 'center',
});
