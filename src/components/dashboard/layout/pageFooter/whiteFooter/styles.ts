import { styled } from '@mui/system';

export const WhiteFooterWrapper = styled('div')({
  backgroundColor: '#F8F8F8',
});

export const WhiteFooterContent = styled('div')({
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'center',
  alignItems: 'center',
  padding: '28px 0rem',
});
